<!-- <!DOCTYPE html>
<html>
<head>
    <title>Gestion des Entreprises</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            height: 100vh;
        }

        h1 {
            color: #333;
            margin: 20px 0;
        }

        #entrepriseList {
            width: 100%;
            max-width: 600px;
            padding: 0;
        }

        .entreprise-item {
            background-color: #fff;
            border: 1px solid #ddd;
            box-shadow: 2px 2px 5px rgba(0,0,0,0.1);
            margin-bottom: 20px;
            padding: 20px;
            border-radius: 5px;
            transition: all 0.3s ease;
            list-style-type: none; /* Retire les puces de la liste */
            position: relative;
        }

        .entreprise-item:nth-child(even) {
            background-color: #f9f9f9;
        }

        .entreprise-item button {
            padding: 10px 15px;
            background-color: #ff4d4d;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            float: right; /* Positionne le bouton à droite */
            margin-top: -10px; /* Ajuste la position du bouton par rapport au haut de l'élément de la liste */
            position: absolute; /* Position absolue pour le bouton */
            top: 25%; /* Déplacer à 50% du haut du parent */
            right: 10px; /* Décaler de 10px du bord droit du parent */
            transform: translateY(-50%); /* Décaler le bouton de -50% de sa propre hauteur pour le centrer verticalement */
        }

        .entreprise-item button:hover {
            background-color: #cc0000;
        }
        #createAccountButton {
            padding: 10px 15px;
            background-color: #4CAF50; /* Vert */
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            margin-bottom: 20px; /* Espace entre le bouton et la liste des entreprises */
        }

        #createAccountButton:hover {
            background-color: #45a049; /* Vert foncé */
        }
    </style>
</head>
<body>
    <h1>Liste des Entreprises</h1>
    <ul id="entrepriseList"></ul>
    <button id="createAccountButton" onclick="window.location.href='/api/users/createUser';">Créer un compte</button>
    <ul id="entrepriseList">
    <script>
        function chargerEntreprises() {
            fetch('http://51.83.69.229:3000/api/users/gestionEntreprise', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                }
            })
            .then(response => response.json())
            .then(data => { 
                const list = document.getElementById('entrepriseList');
                list.innerHTML = ''; // Effacer la liste actuelle
                data.forEach(entreprise => {
                    const item = document.createElement('li');
                    item.className = 'entreprise-item';
                    item.innerHTML = `
                        <strong>Nom de la Firme:</strong> ${entreprise.firm_name}<br>
                        <strong>Prénom:</strong> ${entreprise.first_name}<br>
                        <strong>Nom:</strong> ${entreprise.last_name}<br>
                        <strong>Email:</strong> ${entreprise.email}<br>
                        <strong>Numéro de Téléphone:</strong> ${entreprise.phone_number}<br>
                        <strong>Statut Admin:</strong> ${entreprise.is_admin ? 'Oui' : 'Non'}
                        <button onclick="supprimerEntreprise('${entreprise._id}')">Supprimer</button>
                    `;
                    list.appendChild(item);
                });
            })
            .catch(error => console.error('Erreur:', error));
        }

        function supprimerEntreprise(id) {
            fetch(`http://51.83.69.229:3000/api/users/delete/${id}`, {
                method: 'DELETE'
            })
            .then(response => {
                if (response.ok) {
                    console.log('Entreprise supprimée avec succès');
                    chargerEntreprises(); // Recharger la liste après la suppression
                } else {
                    console.error('Erreur lors de la suppression de l\'entreprise');
                }
            })
            .catch(error => console.error('Erreur:', error));
        }

        window.onload = chargerEntreprises; // Charger les entreprises au chargement de la page
    </script>
    </body>
</html> -->
